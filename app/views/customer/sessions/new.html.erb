<div class="container">
  <br>
  <h2>ログイン</h2>
  <br>
  <h4>会員の方はこちらからログイン</h4>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <div class="field">
        <%= f.label :メールアドレス %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="field">
        <%= f.label :パスワード %><br />
        <%= f.password_field :password, autocomplete: "current-password" %>
      </div>

      <br>
      <div class="actions">
        <%= f.submit "ログイン" %>
      </div>
    <% end %>

  <br>
  <h4>会員登録がお済みでない方</h4>
  <p><%= render "customer/shared/links" %>から新規登録を行なってください。</p>
</div>